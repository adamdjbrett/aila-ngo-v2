---
layout: tags
pagination:
  data: collections.tagPages
  size: 1
  alias: tag
permalink: /tag/{{ tag.slug }}/{% if tag.pageNumber %}page/{{ tag.pageNumber + 1 }}/{% endif %}
eleventyComputed:
  title: '{{ meta.blog.tagSingle }}: {{ tag.name }}'
---

<custom-masonry layout="thirds">
  {% set itemList = tag.pageData %}
  {% asyncEach item in itemList %}
     {% include "partials/card-blog.njk" %}
  {% endeach %}
</custom-masonry>

<h2 class="text-step-2">{{ meta.blog.tagMore }}</h2>

 <!-- set target pagination settings in meta.js -->
  {% set metaKey = "blog" %}
  <!-- if the number of items in the collection is greater than the number of items shown on one page -->
  {% if tag.totalPages !== 0 %}
    <!-- include pagination -->
    {% set keyAlias = tag %}
    {% include 'partials/pagination.njk' %}
  {% endif %}

<ul class="taglist | my-s-m cluster gutter-s" role="list">
  {% for tag in collections.tagList %}
    <li>
      <a
        href="/tag/{{ tag | slugify }}/"
        class="button"
        {{
          helpers.getLinkActiveState('/tag/' + tag | slugify + '/',
          page.url)
          |
          safe
        }}
      >
        {{ tag }}
      </a>
    </li>
  {% endfor %}
</ul>
